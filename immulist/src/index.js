import React from "react";
import ReactDOM from "react-dom";
import 'typeface-roboto';

import SplashInfo from "./SplashScreen";

const rootElement = document.getElementById("root");
ReactDOM.render(<SplashInfo />, rootElement);
